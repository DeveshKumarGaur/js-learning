"use strict";

//alert("Hello") we are using node.js not browser

console.log(3+3)
console.log("Hello")

let name = "Hitesh" // String
let age = 18 // number => 2^53
let isBoolean = false // boolean
let email = null // null datatype have a standalone value which of type object
let password // undefined => value is not defined yet
//symbol is a datatype which is unique

console.log(typeof(age));
console.log(typeof(null));